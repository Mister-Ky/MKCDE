#include <fstream>
#include <vector>

struct Resource {
};

struct MKRF {
};

MKRF readMKRF(const std::string& filename) {
    MKRF mkrf;
    return mkrf;
}

void writeMKRF(const std::string& filename, const MKRF& mkrf) {
}
